{
  "type": "object",
  "required": ["decision", "response"],
  "additionalProperties": false,
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["respond_now", "ask_clarification", "ask_for_roll", "no_action"]
    },
    "response": {
      "type": "string"
    },
    "Skill": {
      "type": "string"
    },
    "Difficulty": {
      "type": "string",
      "enum": ["", "normale", "difficile", "estrema"]
    }
  }
}
